# Zeeq Common ASP.NET Core Library

Contains ASP.NET specific shared common code as it references the ASP.NET framework SDK (whereas `Zeeq.Core.Common` is framework agnostic).

This includes things like base endpoint handler classes, common extensions for working with ASP.NET features, etc.
