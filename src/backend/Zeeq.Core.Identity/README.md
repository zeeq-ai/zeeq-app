# Zeeq Identity

This holds the OpenIddict server implementation for Zeeq auth for both MCP and the API surface area.

Auth is multi-modal meaning that it must support external IdP, internally created tokens, etc.
